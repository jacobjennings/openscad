# Example baseline manifest

`main.json` is a **sample** manifest (captured from a local AppImage) for documentation only.

Live baselines belong in the **`benchmarks/baselines`** git submodule, not in the main OpenSCAD repository. See `benchmarks/README.md` for setup.
